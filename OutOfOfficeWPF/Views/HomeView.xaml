<UserControl x:Class="OutOfOfficeWPF.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OutOfOfficeWPF.Views" xmlns:controls="clr-namespace:OutOfOfficeWPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <controls:NavigationBar Grid.Row="0" DataContext="{Binding NavigationBarViewModel}"></controls:NavigationBar>
        <TextBlock Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top" Text="Home" FontSize="20"></TextBlock>
        <TextBlock Grid.Row="2" Text="{Binding WelcomeInfo}"></TextBlock>
        <TextBlock Grid.Row="3" Text="{Binding Name}" FontSize="20"></TextBlock>
        <TextBlock Grid.Row="4" Text="{Binding Balance}" FontSize="20"></TextBlock>
    </Grid>
</UserControl>
